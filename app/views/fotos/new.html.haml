%h1 New foto

= form_for(@foto, :url => {:foto_id => params[:foto_id], :action => 'create'}, :html => {:multipart => true}) do |f|
  = render(:partial => 'form', :locals => {:f => f})
  .actions
    = f.submit I18n.t('submit')
