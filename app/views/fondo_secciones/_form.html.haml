= form_for @fondo_seccion do |f|
  -if @fondo_seccion.errors.any?
    #error_explanation
      %h2= "#{pluralize(@fondo_seccion.errors.count, "error")} prohibited this fondo_seccion from being saved:"
      %ul
        - @fondo_seccion.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :title
    = f.text_field :title
  .field
    = f.label :slug
    = f.text_field :slug
  .actions
    = f.submit 'Save'
